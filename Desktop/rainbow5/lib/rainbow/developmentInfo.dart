import 'package:flutter/material.dart';

class DevelopmentInfo extends StatelessWidget {
  const DevelopmentInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("development_info"));
  }
}
